name: Python Code Quality Check

on:
  push:
    branches:
    - dev
  workflow_dispatch:
  
jobs:
  quality_check:
    name: Quality Check
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/setup-node@v1
    - name: Use python 3.8
      with:
        python-version: '3.8'
    - name: Install pylint
      run: pip3 install pylint
